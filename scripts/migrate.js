import mysql from "mysql2/promise";import { readdir,readFile } from "node:fs/promises";import { resolve } from "node:path";
const url=process.env.DATABASE_URL||"mysql://rtools:rtools@127.0.0.1:3306/rtools_marketplace",connection=await mysql.createConnection({uri:url,multipleStatements:true});await connection.query("CREATE TABLE IF NOT EXISTS schema_migrations(version VARCHAR(80) PRIMARY KEY,applied_at TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3)) ENGINE=InnoDB");const files=(await readdir(resolve("migrations"))).filter(name=>name.endsWith(".sql")).sort();for(const file of files){const version=file.replace(/\.sql$/,""),[rows]=await connection.execute("SELECT version FROM schema_migrations WHERE version=?",[version]);if(rows[0]){console.log(`- ${version} already applied`);continue;}const sql=await readFile(resolve("migrations",file),"utf8");await connection.beginTransaction();try{await connection.query(sql);await connection.execute("INSERT IGNORE INTO schema_migrations(version) VALUES(?)",[version]);await connection.commit();console.log(`✓ ${version}`);}catch(error){await connection.rollback();throw error;}}await connection.end();
